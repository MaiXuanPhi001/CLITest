import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Tailwind = (): React.JSX.Element => {
  return (
    <View>
      <Text>Tailwind</Text>
    </View>
  )
}

export default Tailwind

const styles = StyleSheet.create({})